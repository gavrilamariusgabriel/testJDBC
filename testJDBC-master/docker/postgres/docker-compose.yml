version: '3.6'

services:

  postgres:
    image: postgres:latest
    container_name: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=marius
      - POSTGRES_PASSWORD=marius
      - POSTGRES_DB=mydatabase

